/* eslint-disable */
const age = 95;
const first = 'alison';
console.log(typeof(first)); // pro tip: use typeof in dev tools to inspect variables on the fly

const smarties = 20;
const kids = 3;
const eachKidGets = smarties / kids;
const nanaGets = smarties % kids;
console.log(`Each kid gets ${Math.floor(eachKidGets)}, Nana gets ${nanaGets}`);

const price = 10.34;
const price_in_cents_bc_integer = 1034;

const notanumber = NaN;
